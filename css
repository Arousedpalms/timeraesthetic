/* style.css */
body {
  font-family: 'Arial', sans-serif;
  background-color: #1e1e1e; /* Dark background, picked from your image */
  color: #c6c6c6; /* Light gray text color for good contrast */
  margin: 0;
  padding: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
}

h1 {
  color: #ffffff; /* Bright white for the main heading */
  margin-bottom: 20px;
}

.progress-bar {
  background-color: #333; /* Darker background for the progress bar */
  border: 1px solid #444; /* Slightly lighter border for some depth */
  border-radius: 5px;
  width: 300px; /* Set a fixed width or use percentages */
  height: 20px;
  margin: 10px 0;
  overflow: hidden;
}

.progress-bar-inner {
  background-color: #bb9353; /* Gold-brown color similar to the bars in the image */
  height: 100%;
  border-radius: 3px;
  transition: width 0.25s ease-in-out; /* Smooth transition for bar animation */
}

/* Add custom classes or IDs for individual progress bars if needed */
/* This way you can customize each bar's color or style separately */

/* If you need to style specific text or elements, add classes or ids to your HTML and reference them here. */

/* Example of a custom class */
.custom-text {
  color: #aabbcc; /* Replace with the actual color you want */
}
