/* Timer Widget Styles */
body {
  font-family: 'Times New Roman', serif; /* or another classic serif font */
  background-color: #f0ead6; /* a light beige background, similar to paper */
  color: #5c5346; /* a dark brown color for text that matches the ink */
  text-align: center;
  padding: 20px;
  margin: 0;
}

#timer {
  font-size: 3em;
  margin: 20px;
  background-color: #dfd3c3; /* a slightly darker beige for the timer background */
  color: #5c5346; /* using the same color as the text for consistency */
  padding: 20px;
  border-radius: 8px;
  display: inline-block;
  box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.1); /* a subtle shadow for depth */
}

.button {
  display: inline-block;
  padding: 10px 20px;
  margin: 10px;
  font-size: 1em;
  background-color: #a4978e; /* a muted, desaturated green */
  color: white;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.button:hover {
  background-color: #8c8379; /* a slightly darker hue for the hover state */
}

.button:active {
  background-color: #746c61; /* even darker for the active state */
}

/* Progress bar container (if you add a progress bar later) */
.progress-container {
  width: 80%;
  background-color: #ddd;
  border-radius: 5px;
  margin: 20px auto;
}

/* Progress bar fill */
.progress-fill {
  height: 20px;
  border-radius: 5px;
  background-color: #a4978e; /* same as button color */
  width: 0%; /* Initial state should have no fill */
  transition: width 0.5s ease-in-out;
}
